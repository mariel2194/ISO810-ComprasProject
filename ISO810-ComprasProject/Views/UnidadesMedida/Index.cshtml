@model IEnumerable<ISO810_ComprasProject.Models.UnidadesMedida>

@{
    ViewData["Title"] = "Index";
}

<h1>Unidades de Medida</h1>

<p>
    <a asp-action="Create">Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Activo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UnidadMedidaId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.UnidadMedidaId">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.UnidadMedidaId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
